# Kindle Agent Frontend

## Setup

1. Install dependencies:

```bash
cd frontend
npm install
```

2. Start the development server:

```bash
npm start
```

3. Open `http://localhost:3000`.

## Notes

- The frontend posts to `http://localhost:8000/upload`.
- Ensure the FastAPI backend is running first.
